
import React from 'react';
import PropTypes from 'prop-types';

const proyecto = props => {
    return (
        <div>
            
        </div>
    );
};

proyecto.propTypes = {
    
};

export default proyecto;